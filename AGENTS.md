# Repository Instructions

- Use `bun` for package scripts and package management.
- Run `bun run test` after changing the ingestion script or tests.
- Keep `SKILL.md` frontmatter valid and the `name` field aligned with the repository folder name.
- Do not commit generated `.codex/video-context/` context packs or `dist/` artifacts unless release packaging explicitly requires them.
